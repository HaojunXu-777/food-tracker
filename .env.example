# Get a free key at https://fdc.nal.usda.gov/api-key-signup.html
USDA_API_KEY=

# --- Optional / disabled by default (paid AI features are off in this release) ---
# Keep these for future re-enablement. Do not use NEXT_PUBLIC_.
# Current free mode does NOT require any AI key.
# AI_API_KEY=
# AI_BASE_URL=
# AI_MODEL=
